import { ColoredAverageDirective } from './colored-average.directive';

describe('ColoredAverageDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoredAverageDirective();
    expect(directive).toBeTruthy();
  });
});
